**タイプ**: 🔍 Concept

## 📝 一行要約
PEP 518/621で標準化されたPythonプロジェクトのメタデータと依存関係を一元管理する設定ファイル

## 🎯 核心的定義
`pyproject.toml`はPythonの公式なプロジェクト設定ファイルで、Node.jsの`package.json`に相当する役割を果たす。TOML形式でプロジェクトメタデータ、依存関係、ビルド設定、開発ツール設定を統一的に記述し、`requirements.txt`の分散的な管理から脱却する。

## 🌟 主な構成要素
- **[project]セクション**: name, version, dependencies, authors等のメタデータ
- **[build-system]セクション**: requires（ビルド依存）、build-backend（setuptools/flit/poetry）
- **[project.optional-dependencies]**: dev/test/docs等の環境別依存関係
- **[tool.*]セクション**: black, mypy, pytest等のツール固有設定

## 🔄 関連概念との違い
`requirements.txt`は単純な依存リストで、`pip freeze`の出力に過ぎず、メタデータやツール設定を含まない。`setup.py`は実行可能Pythonコードで、セキュリティリスクと可読性の問題があった。`pyproject.toml`はこれらを統合し、宣言的（declarative）な設定を実現。Poetry/PDM等のモダンツールは全て`pyproject.toml`を採用している。

## 💡 なぜ重要か
Pythonエコシステムの長年の課題だった「設定ファイル乱立問題」（setup.py, setup.cfg, requirements.txt, MANIFEST.in等）を解決。Node.jsやRustのような統一された開発体験を実現し、プロジェクトの再現性と保守性が飛躍的に向上する。2025年現在、新規プロジェクトでは事実上の標準となっている。

## つながり

← [[MCP Python SDK]]：公式SDKもpyproject.toml採用
↔ [[Python依存関係管理の進化]]：requirements.txtからの移行過程
→ [[Poetry]]：pyproject.toml前提のパッケージマネージャ

## 📚 参照元
> [PEP 518 – Specifying Minimum Build System Requirements](https://peps.python.org/pep-0518/)
> [PEP 621 – Storing project metadata in pyproject.toml](https://peps.python.org/pep-0621/)
